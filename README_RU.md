# KeyWSniper

Этот проект представляет собой мощный инструмент мониторинга Telegram, который объединяет **Юзербота** (для чтения каналов без прав администратора) и **Интерфейс Бота** (для удобного управления через меню).

## Особенности
- 🕵️ **Юзербот:** Отслеживает каналы, в которых вы состоите (права администратора не требуются).
- 🤖 **Интерфейс Бота:** Управляйте ключевыми словами и каналами с помощью удобного меню.
- 🌐 **Мультиязычность:** Поддержка английского, турецкого, русского и немецкого языков.
- 🔔 **Мгновенные уведомления:** Получайте уведомления сразу же при упоминании ключевого слова.
- 📦 **Резервное копирование и восстановление:** Экспорт и импорт вашего списка отслеживания.
- 📝 **Пользовательские заметки:** Добавляйте описания или ссылки к отслеживаемым словам.
- 🗑️ **Интерактивное удаление:** Удаляйте ключевые слова простым нажатием кнопок.

## 🚀 Развертывание на Render.com (24/7)

Чтобы бот работал круглосуточно, выполните следующие действия:

### 1. Подготовка
1. Сделайте Fork этого репозитория.
2. Получите **API_ID** и **API_HASH** на [my.telegram.org](https://my.telegram.org).
3. Получите **BOT_TOKEN** у [@BotFather](https://t.me/BotFather).
4. **Создание сессии:**
   - Запустите `python generate_session.py` на своем компьютере.
   - Войдите по номеру телефона.
   - Скопируйте длинный код, начинающийся с `1BVts...`.

### 2. Настройка Render
1. Создайте новый **Web Service** на Render.
2. Подключите свой репозиторий GitHub.
3. Настройки:
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
4. **Переменные окружения (Environment Variables):**
   
   | Ключ | Значение | Описание |
   | :--- | :--- | :--- |
   | `API_ID` | `123456` | Ваш Telegram API ID |
   | `API_HASH` | `abc123...` | Ваш Telegram API Hash |
   | `BOT_TOKEN` | `123:ABC...` | Токен бота от BotFather |
   | `SESSION_STRING` | `1BVts...` | Код из generate_session.py |

### 3. 💾 Сохранение данных (Важно!)
На Render файлы удаляются при перезапуске. Чтобы не потерять список каналов:

- **Вариант А (Бесплатно):** Используйте кнопку **Backup** в боте. После перезапуска используйте **Import**.
- **Вариант Б (Автоматически):** Бот автоматически сохраняет данные в чат Telegram и восстанавливает их при запуске.
- **Вариант В (Диск):** Добавьте **Disk** в настройках Render (Путь: `/opt/render/project/src`).

## 🔄 Поддержание работы (Keep Alive)

Бесплатный тариф Render переводит сервисы в спящий режим при отсутствии активности. Чтобы этого избежать:

1. Скопируйте URL вашего веб-сервиса Render (например, `https://keywsniper.onrender.com`).
2. Перейдите на [UptimeRobot](https://uptimerobot.com/) и создайте бесплатный аккаунт.
3. Нажмите **"Add New Monitor"**.
   - **Monitor Type:** HTTP(s)
   - **Friendly Name:** KeyWSniper
   - **URL:** Вставьте ваш URL Render
   - **Monitoring Interval:** 5 minutes
4. Сохраните. UptimeRobot будет пинговать ваш бот каждые 5 минут, чтобы он не засыпал.

## Локальная установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/siimsek/KeyWSniper.git
   cd KeyWSniper
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Запустите бота:**
   ```bash
   python main.py
   ```

## 🐳 Поддержка Docker

Вы также можете запустить бота с помощью Docker:

```bash
docker build -t keywsniper .
docker run --env-file .env keywsniper
```
